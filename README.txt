To execute the program:

python PegSolitaireSolver -i <inputFile> -a <algorithm> -h <heuristic>

algorithm can be:

1. IDS (Iterative deepening)
2. aStar

heuristic parameter only valid for aStar can take two values:

1. WCM (Weighted Costv Matrix)
2. MD (Manhattan Distance)